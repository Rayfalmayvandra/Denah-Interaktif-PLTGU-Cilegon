services:
  web-visualisasi-pltgu:
    build:
      context: .
      dockerfile: Dockerfile
    image: visualisasi-pltgu:latest
    container_name: denah-pltgu-container
    restart: always
    ports:
      - "3004:80"       # Port 3004 diakses dari luar, diteruskan ke port 80 Nginx di dalam container
    environment:
      - TZ=Asia/Jakarta
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:80/index.html"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
